# Prod overlay: 48-core 96GB node
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - target:
      kind: Deployment
      name: combined-engine
    patch: |
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: combined-engine
        namespace: quant
      spec:
        template:
          spec:
            containers:
              - name: combined-engine
                env:
                  - name: FACTOR_WORKERS
                    value: "46"
                resources:
                  requests:
                    cpu: "4"
                    memory: 64Gi
                  limits:
                    cpu: "47"
                    memory: 90Gi
